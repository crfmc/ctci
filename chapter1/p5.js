/**
 * Question:
 * There are three types of edits that can be performed on strings:
 * insert a character, remove a character, or replace a character.
 * Given two strings, write a function to check if they are one edit
 * (or zero edits) away.
 */

/**
 * Notes:
 */
 const Problem5 = (s) => {
    return "unimplemented"
  }
  
  
  // Tests for Problem
  console.log("Problem 5 Tests running... \n");
  
  console.assert( Problem5("pale", "ple") === true, "ex1");
  console.assert( Problem5("pales", "pale") === true, "ex2");
  console.assert( Problem5("pale", "bale") === true, "ex3");
  console.assert( Problem5("pale", "bake") === false, "ex4");
  
  
  
  module.exports;
  
  /**
   * Notes:
   */
  